function showInfo(element) {
    const infoElement = element.querySelector('.info');
    infoElement.style.display = 'block';
}

function showMoreInfo(element) {
    // Aquí puedes agregar el código para mostrar más información sobre la imagen al hacer clic en el botón.
    // Puede ser mediante un cuadro de diálogo modal o desplazando la página para mostrar más detalles.
    // Por ejemplo:
    // alert("Más información de la imagen: " + element.previousElementSibling.innerHTML);
}
